#!/bin/bash
JSON="$(curl -s https://ion.tjhsst.edu/api/schedule?format=json)"
PARSE="$(echo $JSON | grep -Po  '{\"order\":[^,]*,\"name\":[^,]*,\"start\":[^,]*,\"end\":[^}]*}')"
#PARSE="$(echo $JSON | grep -P  '/.*/')"
echo $PARSE
